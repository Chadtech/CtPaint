verticalColorSwapPosture = [
  (event) ->
    drawInformation(event)
    setCasualPosition(event)
  (event) ->
    mousePressed = true
  (event) ->
    mousePressed = false
    drawToolbars()
  (event) ->
]

