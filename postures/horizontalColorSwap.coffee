horizontalColorSwapPosture = [
  (event) ->
    setCasualPosition(event)
    drawInformation(event)
  (event) ->
    mousePressed = true
  (event) ->
    mousePressed = false
    drawToolbars()
  (event) ->
]

